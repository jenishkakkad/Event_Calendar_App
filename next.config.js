/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // Optional: if you want to allow external images later
  images: {
    domains: [],
  },
};

module.exports = nextConfig;
